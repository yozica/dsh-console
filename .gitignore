# 依赖与产物
node_modules/
dist/
release/
.npm-cache/
.electron-cache/
.downloads/
.verify/
.verify-userdata/
# 本地验证 mac 打包时给 electron-builder 用的临时 HOME（它会往 ~/Library/Caches 写东西）
.build-home/
*.log

# 打包时留下的调试配置（electron-builder 每次生成）
builder-debug.yml
builder-effective-config.yaml

# 编辑器
.vscode/
.idea/

# 开发这个仓库时用的临时工具与截图（不属于应用本身）
.dsh/
